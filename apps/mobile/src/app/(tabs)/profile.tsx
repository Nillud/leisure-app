import { Text } from 'react-native'

import { Screen } from '@/components/Screen'

export default function Profile() {
    return (
        <Screen>
            <Text>Профиль</Text>
        </Screen>
    )
}
